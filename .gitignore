# Environment & Secrets
.env
.env.local

# macOS & IDE
.DS_Store
.idea/
.vscode/

# Node
node_modules/
npm-debug.log*

# Python
__pycache__/
*.pyc
*.pyo

# Newton backend database (contains user data — never commit)
backend/users.db
backend/.env
backend/

# iOS & Xcode
build/
DerivedData/
*.xcarchive
*.ipa
.swiftpm/

# Newton debug dumps (never commit)
/yap.txt
/log.txt
/*.log

# Local helper scripts (never commit, may contain machine-specific paths)
/push.sh

# Android (local SDK, build outputs, wrapper binaries)
/local.properties
android/local.properties
android/.gradle/
android/build/
android/shared/build/
android/app/build/
android/gradle/wrapper/gradle-wrapper.jar
*.apk
*.aab
*.ap_
.gitignore
training/
scripts/
.github/
.claude/
live-server.json
*.zip